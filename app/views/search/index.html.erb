<h1>Search for Anything!</h1>

<br />


<%= form_tag(searchByName_path, :method => "get", id: "search-form") do %>
<div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Search for Recipes by Dish Name </div>
  <div class="card-body text-secondary">
      <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "Search By Name", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
  </div>
    <% end %>

</div>

<br />

 <%= form_tag(searchByIngredients_path, :method => "get", id: "search-form") do %>
 <div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Search for Recipes by Ingredient </div>
  <div class="card-body text-secondary">
      <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "Search By Ingredients", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
  </div>
    <% end %>

</div>

<br />

 <%= form_tag(searchWinePairing_path, :method => "get", id: "search-form") do %>
 <div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Search for a wine pairing </div>
  <div class="card-body text-secondary">
      <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "Search Wine Pairing", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
  </div>
<% end %>

</div>

<br />

<%= form_tag(searchVideo_path, :method => "get", id: "search-form") do %>
<div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Search for Recipe videos </div>
  <div class="card-body text-secondary">
      <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "Search Videos", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
    </div>
<% end %>

</div>

<br />

<%= form_tag(convertUnits_path, :method => "get", id: "search-form") do %>
<div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Convert recipe amounts </div>
  <div class="card-body text-secondary">
      <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "convert #, c to g", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
  </div>
<% end %>

</div>

<br />

<%= form_tag(searchCalories_path, :method => "get", id: "search-form") do %>
<div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Search Recipes for daily, weekly, or monthly calorie needs </div>
  <div class="card-body text-secondary">
      <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "number of cals, per time", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
  </div>
<% end %>

</div>

<br />

<%= form_tag(searchRecipeId_path, :method => "get", id: "search-form") do %>
<div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Search by Recipe ID and get similar recipes </div>
  <div class="card-body text-secondary">
      <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "enter any recipe id", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
  </div><% end %>
</div>

<br />

<%= form_tag(getFoodTrivia_path, :method => "get", id: "search-form") do %>
<div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Click search for food trivia </div>
  <div class="card-body text-secondary">
      <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "click search", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
  </div>
<% end %>
</div>

<br />



<%= form_tag(searchQuickAnswer_path, :method => "get", id: "search-form") do %>
<div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Enter any question to search quickly </div>
  <div class="card-body text-secondary">
      <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "enter question here", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
  </div>
<% end %>
</div>

<br />

<%= form_tag(searchNutritionInfo_path, :method => "get", id: "search-form") do %>
<div class="card border-secondary mb-3 search-card" style="max-width: 18rem;">
  <div class="card-header">Search nutrition info by dish name </div>
  <div class="card-body text-secondary">
    <%= text_field_tag :searchTerm, params[:searchTerm], placeholder: "enter dish name", :class => "form-control" %>
      <%= submit_tag "Search", :name => nil, :class => "btn btn-outline-primary search-button" %>
</div>

<% end %>
</div>

